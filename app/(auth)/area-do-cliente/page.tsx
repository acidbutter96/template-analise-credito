export const metadata = {
  title: "Área do cliente — CrediCheck",
  description: "Mock de login e dashboard para um template de análise de crédito.",
};

import AreaDoClienteClient from "./AreaDoClienteClient";

export default function AreaDoClientePage() {
  return <AreaDoClienteClient />;
}
